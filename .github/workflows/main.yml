name: Github 52-Sign-Bot

on:
    push:
        branches:
            - master
    schedule:
        - cron: '30 23 * * *'
jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout codes
              uses: actions/checkout@v2
            - name: Use Node.js
              uses: actions/setup-node@v1
              with:
                node-version: '12.x'
            - name: Run app
              run: npm install
            - run: node app.js
              env:
                WUAI_COOKIE: ${{ secrets.WUAI_COOKIE }}
                WEICHAT_CORPID: "ww578bc7099b9817b5"
                WEICHAT_CORPSECRET: "Bd2_Qdrbro7cQ4M-fLshuYqT_FqBVeTBSXi-kWpl1H4"
                WEICHAT_AGENTID: 1000003
        
#                 WEICHAT_CORPID: ${{ secrets.WEICHAT_CORPID }}
#                 WEICHAT_CORPSECRET: ${{ secrets.WEICHAT_CORPSECRET }}
#                 WEICHAT_AGENTID: ${{ secrets.WEICHAT_AGENTID }}
